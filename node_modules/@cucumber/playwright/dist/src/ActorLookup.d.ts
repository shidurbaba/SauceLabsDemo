import Actor from './Actor';
export default class ActorLookup {
    private readonly actorByName;
    findOrCreateActor(world: unknown, actorName: string): Actor;
    get actors(): IterableIterator<Actor>;
}
